python -m pip --version: Usado para verificar a versão do pip instalada.
python -m pip install <nome_do_pacote>: Usado para instalar um pacote Python.
python -m pip install --upgrade pip: Usado para atualizar o próprio pip.
